{
  "name": "film-scheduler-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter film-scheduler dev",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "next": "14.2.15",
    "tsx": "^4.19.4"
  }
}